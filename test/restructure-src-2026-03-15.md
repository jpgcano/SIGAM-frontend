# Evidencia - Reencarpetado src

Fecha: 2026-03-15

## Objetivo
Modularizar el frontend con carpetas `services` y `state`.

## Pasos
1. Mover servicios (`api-client.js`, `config.js`, `env.js`, `api.js`) a `src/services/`.
2. Mover almacenamiento a `src/state/`.
3. Actualizar imports.

## Resultado
Estructura organizada por responsabilidad.
