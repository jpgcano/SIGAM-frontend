{
  "name": "sigam-frontend",
  "private": true,
  "scripts": {
    "build": "node scripts/build-env.js",
    "test": "vitest",
    "test:watch": "vitest --watch"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vitest": "^1.6.0"
  }
}
